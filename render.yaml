services:
  # Web Service - Development/Staging Application
  # Blueprint sync trigger
  - type: web
    name: clearpay-web-dev
    env: docker
    plan: free  # Options: free, starter, standard, pro
    region: oregon  # Options: oregon, frankfurt, singapore, mumbai, tokyo
    dockerfilePath: ./Dockerfile
    dockerContext: .
    healthCheckPath: /health.php
    branch: development  # Deploy from development branch
    envVars:
      - key: CI_ENVIRONMENT
        value: development  # Development environment
      - key: APP_TIMEZONE
        value: Asia/Manila
      # Database connection will be set from the database service
      - key: DATABASE_URL
        fromDatabase:
          name: clearpay-db-dev
          property: connectionString
      # Encryption key will be auto-generated by docker-entrypoint.sh if not set
      # You can also set it manually in Render dashboard for consistency across deployments
      # Format: base64:YOUR_BASE64_KEY_HERE
    # Auto-deploy from development branch
    autoDeploy: true

databases:
  # PostgreSQL Database for Development (Render free tier only supports PostgreSQL)
  - name: clearpay-db-dev
    databaseName: clearpaydb_dev
    user: clearpay_user_dev
    plan: free  # Options: free, starter, standard, pro
    region: oregon  # Should match web service region

